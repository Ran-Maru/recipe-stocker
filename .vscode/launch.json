{
  "version": "0.2.0",
  "configurations": [
    {
      // editor-browserだとブレークポイントがうまく動作しなかった。
      // "type": "editor-browser",
      "type": "chrome",
      "request": "launch",
      "name": "Vite: Chromeデバッグ (先にnpm run devが必要)",
      "url": "http://localhost:5173",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
